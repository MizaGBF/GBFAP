define(["underscore"], (function() {
	// unused, here just to not break compatibility
	// NOTE: these functions are effects used in raids (unlike the gacha demo).
	// I tried implementing a few to test, once, and the result wasn't worth of the effort.
	// It doesn't mean you shouldn't try.
	return {
		hideHUD: function() {},
		showHUD: function() {},
		mDamage: function() {},
		mDamageSyncEffect: function() {},
		_changeDamageMotionBoss: function() {},
		_changeHpBoss: function() {},
		mChangeMotion: function() {},
		mMoveTo: function() {},
		mAnimColor: function() {},
		mAnimBrightness: function() {},
		mAnimShake: function() {},
		mAnimZoomChara: function() {},
		mAnimZoomReset: function() {}
	}
}));